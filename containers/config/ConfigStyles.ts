import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
  avatar:{
    alignItems:'center',    
    marginBottom:30,
    marginTop:30
  }
});
